#!/usr/bin/env ruby
require_relative '../lib/js_generator'
if ARGV.length < 3
  raise ArgumentError.new("wrong number of arguments (given #{ARGV.length}, expected 3)")
end

setup_js = JsGenerator::SetupJs.new(ARGV[0], ARGV[1], ARGV[2])
setup_js.run
